Act as an expert Embedded Systems and IoT Architect. I want you to write a complete, production-ready, well-structured Git project repository from scratch for an "Ultra-Low-Power Dual-MCU IoT Telemetry Node (STM32 & ESP32)".

Please generate the complete folder structure, configuration files, and source code files implementing the exact architectural specifications listed below:

### Architecture & Technical Requirements:

1. Dual-MCU Setup:
   - MCU 1 (STM32): Handles low-level bare-metal peripherals, sensor/battery data acquisition, motor/actuator states, and aggressive power management.
   - MCU 2 (ESP32): Handles wireless connectivity, local buffering, and cloud communication via MQTT.

2. STM32 Bare-Metal & Ultra-Low-Power Firmware:
   - Implement an event-driven architecture using STM32 Low-Power / Stop Mode.
   - Implement aggressive peripheral clock gating and set unused GPIOs to Analog mode to eliminate leakage currents, reducing active-to-sleep current drain to sub-10 µA.
   - Configure a background data acquisition pipeline using ADC and DMA circular buffers so the CPU remains in deep sleep for over 99% of the operational lifecycle.

3. Secure Inter-MCU Communication Protocol:
   - Design a secure, lightweight UART handshake protocol between the STM32 and ESP32.
   - The STM32 must trigger the ESP32 to transmit telemetry data only when data packets are fully assembled and ready in RAM.

4. MQTT Telemetry Pipeline:
   - Implement client logic on the ESP32 to format telemetry packages (JSON/Binary structs) and securely publish them to an MQTT broker.

### Expected Repository Structure & Deliverables:

Please provide the contents of the following directory tree with clean, well-commented, professional C/C++ code:

- /stm32_firmware/
  - include/ (power_manager.h, dma_adc.h, uart_handshake.h)
  - src/ (main.c, power_manager.c, dma_adc.c, uart_handshake.c)
  - CMakeLists.txt or Makefile
- /esp32_firmware/
  - main/ (main.cpp, wifi_mqtt_client.cpp, uart_receiver.h)
  - CMakeLists.txt (ESP-IDF format)
- /common/
  - protocol_spec.h (Shared structs and UART framing protocol specification)
- README.md (Comprehensive documentation explaining architecture, low-power strategies, current optimization, and setup instructions)

Write clean, modular code following embedded industry best practices (MISRA-aligned style, register-level or lightweight HAL abstractions for power modes, proper memory alignment for DMA). Provide the code blocks sequentially so I can directly create the Git repository.
